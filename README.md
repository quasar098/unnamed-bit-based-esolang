# unnamed bit based esolang

the concept is that even on golfing languages like vyxal and golfscript, using entire characters to represent really common information is bad. instead of byte-based tokenization, we can use a huffman table.

